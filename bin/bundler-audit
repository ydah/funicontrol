#!/usr/bin/env ruby
require_relative "../config/boot"
require "bundler/audit/cli"

if ARGV.empty? || ARGV.include?("check")
  ARGV.concat %w[--config config/bundler-audit.yml --database tmp/ruby-advisory-db]
end
Bundler::Audit::CLI.start
